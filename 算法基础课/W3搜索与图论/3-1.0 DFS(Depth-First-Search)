深度优先:  用于遍历所有成功情况
  利用stack先入后出的性值,实现深度搜索的性值
  当前条件满足,结点储存进入stack并且递归进入下一层;当前条件不满足,stack结点弹出并且恢复现场退回上一层
  
特点:
  Space Complexity :  O(h)  h为深度
  第一次搜索成功不一定为最短距离

判断-某节点符合条件{
  录入该结点;
  标记该结点;
  向下遍历();
  删除结点标记(恢复初始状态);
}
