假设: n个点, m条边

Kruskal算法的基本步骤:  
  1.0 所有边从从大到小排序  O(m*logm)
  2.0 枚举每条边  O(m)
    假设点a和点b存在权重为c的边,若a,b不联通(c==0x3f3f3f3f),则将这条边加入到集合(已经建成的树)中



问题:如何将建立结合储存构建树的结点
  利用并查集来储存结点
问腿:如何判断最终建立的树是否成立
  两个变量,在枚举每条边的时候
    cnt记录加入集合(已经建成的树)的边数   ==>  树建立的标准为 树边 == n-1
    res累加记录加入集合(已经建成的树)的权重
    
