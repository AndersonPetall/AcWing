源点:起点   汇点:终点   
假设: n个点, m条边
稠密度: m ~ n^2    点少变多
稀密度: m ~ n    点多边少

最短路问题:
  单源最短路: 一个点到其他所有点的最短路劲
    所有边权为正
      朴素Dijkstra算法  ==>  O(n^2)       特点:与边数无关,适合稠密度(边数较多的情况)
      堆优化Dijkstra算法  ==>  O(m*logn)  特点:适合于稀密度
    存在边权为负
      Bellman-Ford  ==>  O(m*n)           特点:设用于路劲边数有限, EG:不超过N条边
      SPFA  ==>  一般平均O(m), 最坏O(m*n)  特点:效率更高,但不适合所有情况
  多源最短路: 源点和汇点不确定
    Floyd算法  ==>  O(n^3)
  

