假设: n个点, m条边

思路: 循环n1中每个结点
  重置标记为FALSE
  n1中结点N1寻找匹配
    若n2中N2结点标记为FALSE,则修改为TRUE
      若N2结点当前没有匹配对象,或者N2结点当前的匹配对象可以更换匹配,则修改N2结点的匹配对象为当前结点N1
      

构建:
  链表储存边
  match[N2] = N1  表示n2中结点N2的匹配为n1中的结点N1
  st[N2] = true/false  表示对于n2中结点N2的标记
  bool find(int pointIdx)  判断在当前的情况下,n1中的结点是否能够找到匹配

PS:
  如何判断n2中的结点N2的当前匹配对象可以更换匹配  
    ==>  递归调用find(match[N2])函数

  无向图,但是只用储存从n1中的结点到n2中的结点的边
    ==>  匹配方式为从n1中某结点寻找n2中有无匹配的结点
