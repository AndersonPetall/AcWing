目的: 在不超过 K 条边的情况下,求最短路问题
  若存在负权边,没有边数的规定,则最短路问题不一定存在(路劲上有负环)  
  ==>  若在 K+X 条边的情况下,最短路问题有更新,则说明存在负环

基本步骤
  for循环K次 
    for循环 遍历所有边 
      对于 a ==w==> b  更新 dis[b] = min(dis[b], dis[a] + w)   <==  松弛操作
  完成循环后,对于所有边必定满足 dis[b] <= dis[a] + w;    <==  三角不等式
  
