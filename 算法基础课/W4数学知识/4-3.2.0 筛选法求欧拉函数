4-3.0 欧拉函数  ==>  ϕ(N) = N(整数项) * 质数系数


欧拉函数的性质  假设  C = A * B
性质一:  若 A 为质数, 则ϕ(A) = A - 1
证明:   
  ϕ(A) = A * 质数系数 = A * (1 - 1/A) = A - 1;
  
性质二:  若 A 为合数, 则ϕ(A) = A * 质数系数
  
性质三:  若 C=A*B, A和B 互质, 则ϕ(C) = ϕ(A) * ϕ(B)
证明:   
  ϕ(C) = ϕ(A * B) 
       = (A * B) * [(A * B)的质数系数]  A和B互质,则其质数系数不存在相同项
       = A * A的质数系数 * B * B的质数系数
       = ϕ(A) * ϕ(B)
       
性质四:  若 C=A*B, A和B 不互质, 且B为A的最小质因子, 则ϕ(C) = ϕ(A) * B
证明:   
  ϕ(C) = ϕ(A * B) 
       = (A * B) * [(A * B)的质数系数]  B为A的最小质因子,则 (A*B)的质数系数和 A的质数系数相同(幂不同)
       = (A * B) * A的质数系数
       = ϕ(A) * B
(若B不为A的最小质因子, C的质数系数和A的质数系数相同(幂不同), 但是ϕ(C)的整数项不能表示为A*B)


思路:  利用线性筛法的思想, 根据质数和当前的数计算出后续的数
  回顾  4-1.3.0 筛选质数_线性筛法

构建:  
回顾 4-1.3.0 筛选质数_线性筛法
primes[]数组储存质数
eulers[]数组储存整数i的欧拉函数

从小到大遍历所有数
  若当前数i没有标记,则为质数
    primes[]录入
    eulers[]计算
  利用当前数i和质数数组进行操作 t = i * primes
    对t进行标记(合数)
    若primes和i互质
      eulers[]计算
    若primes和i不互质, 且primes为i的最小质数
      eulers[]计算; 退出循环;
