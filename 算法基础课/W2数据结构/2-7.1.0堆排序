思路:
  从 n/2 开始down, 时间复杂度 为 O(n)  ==> 从最后一个分支结点开始down
    当索引从 0 开始,则判断条件为 i >= 0, 根节点索引为0
    当索引从 1 开始,则判断条件为 i>0(简写为 i ), 根节点索引为1
  下调函数down 中
    t记录父节点与两个子节点中最小值的索引, t 的初始值为父节点索引, 一旦改变则意味需要向下调整(swap)
    swap 是交换两个变量的值,两个变量的索引不变,所以迭代条件为 down(t)
